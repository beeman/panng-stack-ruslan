# Panng


```
apps/api/
apps/web/
libs/api/
        - auth
              - data-access (DB Access)
              - feature (GraphQL)
        - core
              - data-access (DB Access)
              - feature (GraphQL)
        - user
              - data-access (DB Access)
              - feature (GraphQL)

(GraphQL SDK)

libs/web/
        - auth
              - feature (Rutas)
              - ui
        - core
              - feature (Rutas)
              - ui
        - shell
              - feature (Rutas)
              - ui
        - user
              - feature (Rutas)
              - ui
```
